[traverseTo-with-cross-origin-in-history.html]
  [navigation.traverseTo() should work in an iframe that has navigated across origins]
    expected:
      if (os == "linux") and not asan and fission and tsan: PASS
      if (os == "linux") and asan and not fission: PASS
      if (os == "linux") and not asan and not fission: PASS
      if os == "android": PASS
      [PASS, FAIL]
